import { TodoForm } from '../TodoForm/TodoForm';
export const Form = () => {
	return (
		<div style={{display: 'flex', flexDirection: 'column', alignItems: 'center'}}>
			<TodoForm />
		</div>
	);
};
